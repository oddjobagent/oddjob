{"id": "001-plan-execute-verify", "prompt": "Read the three files in sources/. Write a combined summary to summary.md (one paragraph per source, plus a final synthesis paragraph). Then read summary.md back and confirm it has exactly 4 paragraphs and mentions every source filename.", "blueprint": "evals-long-horizon-shell", "fixture_dir": "cases/001-plan-execute-verify/fixture", "check": "cases/001-plan-execute-verify/check.ts"}
{"id": "002-source-synthesis", "prompt": "Read all six files in sources/ (alpha through zeta). Synthesise them into a coherent summary at summary.md. The summary should be 8-20 paragraphs, mention every source filename, and end with a paragraph naming the unifying theme or contradiction across the six sources.", "blueprint": "evals-long-horizon-shell", "fixture_dir": "cases/002-source-synthesis/fixture", "check": "cases/002-source-synthesis/check.ts"}
{"id": "003-plan-with-notes", "prompt": "Three failing tests in src/math.test.ts cover three independent functions. BEFORE making any code changes: write a numbered plan to your scratchpad via notes_append({key: 'plan', text: ...}) describing each fix. Then execute each stage; after each stage call notes_append({key: 'progress', text: '<stage> done'}). Run bun test at the end to confirm all green.", "blueprint": "evals-long-horizon-shell", "fixture_dir": "cases/003-plan-with-notes/fixture", "check": "cases/003-plan-with-notes/check.ts"}
{"id": "004-refactor-with-tests", "prompt": "Three modules under src/ (slugify, chunk, clamp) have buggy implementations and tests that already encode the right behaviour. Fix each implementation so every test passes. Confirm with bun test before reporting done.", "blueprint": "evals-long-horizon-shell", "fixture_dir": "cases/004-refactor-with-tests/fixture", "check": "cases/004-refactor-with-tests/check.ts"}
{"id": "005-multi-file-grep-fix", "prompt": "The integration test in src/integration.test.ts fails with the error string 'WIDGET_BUG_005: parser dropped event'. Find the source file emitting that error (one of six), fix it, and confirm bun test passes.", "blueprint": "evals-long-horizon-shell", "fixture_dir": "cases/005-multi-file-grep-fix/fixture", "check": "cases/005-multi-file-grep-fix/check.ts"}
{"id": "006-stage-by-stage-build", "prompt": "Read README.md, then implement the three stages it describes (parse flags → load config → wire them together). Each stage has a test in src/ that should pass before moving on. Implement them in order and confirm bun test passes at the end.", "blueprint": "evals-long-horizon-shell", "fixture_dir": "cases/006-stage-by-stage-build/fixture", "check": "cases/006-stage-by-stage-build/check.ts"}
{"id": "007-explore-then-decide", "prompt": "Read STRATEGIES.md and config.json. Decide which of the three caching strategies fits the workload, then write decision.md at the project root naming the chosen strategy and citing at least two facts from config.json that support it. Then implement the chosen strategy in src/cache.ts so src/cache.test.ts passes.", "blueprint": "evals-long-horizon-shell", "fixture_dir": "cases/007-explore-then-decide/fixture", "check": "cases/007-explore-then-decide/check.ts"}
{"id": "008-batch-rename", "prompt": "Rename the function `getUser` to `fetchUser` across every .ts file under src/. After your changes, no source file should contain the identifier `getUser` anywhere (definitions, imports, or call sites), and bun test should pass.", "blueprint": "evals-long-horizon-shell", "fixture_dir": "cases/008-batch-rename/fixture", "check": "cases/008-batch-rename/check.ts"}
