name = "digest-pipeline-joiner"
version = "0.1.0"
description = "Final stage: concatenates the per-source summaries into a markdown digest."
author = "demo"
tags = ["script-mode", "demo"]
license = "MIT"

[entry]
runtime = "bun"
file = "main.ts"
