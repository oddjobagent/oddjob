import { defineCommand } from "citty";

export default defineCommand({
  meta: { name: "inspect", description: "TODO: inspect command" },
  async run() {
    throw new Error("oddjob inspect: not implemented (see plan)");
  },
});
