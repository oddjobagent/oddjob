import { defineCommand } from "citty";

export default defineCommand({
  meta: { name: "serve", description: "TODO: serve command" },
  async run() {
    throw new Error("oddjob serve: not implemented (see plan)");
  },
});
