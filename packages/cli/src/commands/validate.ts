import { defineCommand } from "citty";

export default defineCommand({
  meta: { name: "validate", description: "TODO: validate command" },
  async run() {
    throw new Error("oddjob validate: not implemented (see plan)");
  },
});
