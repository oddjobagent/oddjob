import { defineCommand } from "citty";

export default defineCommand({
  meta: { name: "output", description: "TODO: output command" },
  async run() {
    throw new Error("oddjob output: not implemented (see plan)");
  },
});
