import { defineCommand } from "citty";

export default defineCommand({
  meta: { name: "push", description: "TODO: push command" },
  async run() {
    throw new Error("oddjob push: not implemented (see plan)");
  },
});
