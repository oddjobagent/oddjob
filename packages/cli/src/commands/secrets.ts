import { defineCommand } from "citty";

export default defineCommand({
  meta: { name: "secrets", description: "TODO: secrets command" },
  async run() {
    throw new Error("oddjob secrets: not implemented (see plan)");
  },
});
