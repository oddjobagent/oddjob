import { defineCommand } from "citty";

export default defineCommand({
  meta: { name: "setup", description: "TODO: setup command" },
  async run() {
    throw new Error("oddjob setup: not implemented (see plan)");
  },
});
