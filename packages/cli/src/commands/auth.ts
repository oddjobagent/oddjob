import { defineCommand } from "citty";

export default defineCommand({
  meta: { name: "auth", description: "TODO: auth command" },
  async run() {
    throw new Error("oddjob auth: not implemented (see plan)");
  },
});
