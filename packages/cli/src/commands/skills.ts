import { defineCommand } from "citty";

export default defineCommand({
  meta: { name: "skills", description: "TODO: skills command" },
  async run() {
    throw new Error("oddjob skills: not implemented (see plan)");
  },
});
