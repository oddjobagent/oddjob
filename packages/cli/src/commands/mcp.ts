import { defineCommand } from "citty";

export default defineCommand({
  meta: { name: "mcp", description: "TODO: mcp command" },
  async run() {
    throw new Error("oddjob mcp: not implemented (see plan)");
  },
});
