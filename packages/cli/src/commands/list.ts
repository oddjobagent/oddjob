import { defineCommand } from "citty";

export default defineCommand({
  meta: { name: "list", description: "TODO: list command" },
  async run() {
    throw new Error("oddjob list: not implemented (see plan)");
  },
});
