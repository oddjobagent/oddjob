import { defineCommand } from "citty";

export default defineCommand({
  meta: { name: "logs", description: "TODO: logs command" },
  async run() {
    throw new Error("oddjob logs: not implemented (see plan)");
  },
});
