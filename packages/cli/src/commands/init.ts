import { defineCommand } from "citty";

export default defineCommand({
  meta: { name: "init", description: "TODO: init command" },
  async run() {
    throw new Error("oddjob init: not implemented (see plan)");
  },
});
