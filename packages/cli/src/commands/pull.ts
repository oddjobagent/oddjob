import { defineCommand } from "citty";

export default defineCommand({
  meta: { name: "pull", description: "TODO: pull command" },
  async run() {
    throw new Error("oddjob pull: not implemented (see plan)");
  },
});
