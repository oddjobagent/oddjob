import { defineCommand } from "citty";

export default defineCommand({
  meta: { name: "run", description: "TODO: run command" },
  async run() {
    throw new Error("oddjob run: not implemented (see plan)");
  },
});
