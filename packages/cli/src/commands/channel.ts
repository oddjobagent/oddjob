import { defineCommand } from "citty";

export default defineCommand({
  meta: { name: "channel", description: "TODO: channel command" },
  async run() {
    throw new Error("oddjob channel: not implemented (see plan)");
  },
});
