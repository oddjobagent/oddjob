import { defineCommand } from "citty";

export default defineCommand({
  meta: { name: "deploy", description: "TODO: deploy command" },
  async run() {
    throw new Error("oddjob deploy: not implemented (see plan)");
  },
});
