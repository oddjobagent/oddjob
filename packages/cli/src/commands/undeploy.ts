import { defineCommand } from "citty";

export default defineCommand({
  meta: { name: "undeploy", description: "TODO: undeploy command" },
  async run() {
    throw new Error("oddjob undeploy: not implemented (see plan)");
  },
});
