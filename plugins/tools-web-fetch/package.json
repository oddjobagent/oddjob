{
  "name": "@oddjob/plugin-tools-web-fetch",
  "version": "0.0.0",
  "description": "Oddjob plugin: web_fetch tool + bundled backends (raw, Browserbase, Firecrawl, ScrapingBee)",
  "type": "module",
  "main": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "test": "bun test",
    "typecheck": "bunx tsgo --noEmit",
    "build": "bunx tsgo --build",
    "clean": "rm -rf dist *.tsbuildinfo"
  },
  "dependencies": {
    "@mariozechner/pi-agent-core": "^0.70.2",
    "@oddjob/agent": "workspace:*",
    "@oddjob/core": "workspace:*",
    "@oddjob/sdk": "workspace:*",
    "turndown": "^7.2.0",
    "typebox": "^1.1.33"
  },
  "devDependencies": {
    "@types/turndown": "^5.0.5"
  }
}
