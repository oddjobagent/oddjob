{
  "name": "@oddjob/plugin-pi-models",
  "version": "0.0.0",
  "description": "Oddjob plugin: registers a ModelProviderService for every pi-ai built-in provider (Anthropic, OpenAI, OpenRouter, Google, Mistral, Bedrock, Groq, Cerebras, X.AI, Kimi, Vercel AI Gateway, ...). Replaces the per-provider plugins.",
  "type": "module",
  "main": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "test": "bun test",
    "typecheck": "bunx tsgo --noEmit",
    "build": "bunx tsgo --build",
    "clean": "rm -rf dist *.tsbuildinfo"
  },
  "dependencies": {
    "@mariozechner/pi-ai": "^0.70.2",
    "@oddjob/core": "workspace:*",
    "@oddjob/sdk": "workspace:*"
  }
}
