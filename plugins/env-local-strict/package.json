{
  "name": "@oddjob/plugin-env-local-strict",
  "version": "0.1.0",
  "description": "Oddjob plugin: local-strict environments (seatbelt on Mac, bwrap on Linux, appcontainer on Win).",
  "type": "module",
  "main": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "test": "bun test",
    "typecheck": "bunx tsgo --noEmit",
    "build": "bunx tsgo --build",
    "clean": "rm -rf dist *.tsbuildinfo"
  },
  "dependencies": {
    "@oddjob/core": "workspace:*",
    "@oddjob/sdk": "workspace:*"
  }
}
