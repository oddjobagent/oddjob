slug = "env-docker"
name = "Docker environment"
description = "Container environment backed by the local Docker daemon. One container per Run, mounted workdir, --rm cleanup. Trust tier: container."
version = "0.1.0"
author = "Oddjob"
entry = "src/index.ts"

[oddjob]
engine = "*"
